<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Tema normal de la app -->
    <style name="Theme.Wahsly" parent="android:Theme.Material.Light.NoActionBar" />

    <!-- Tema de la splash screen -->
    <style name="Theme.Wahsly.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">#FFFFFF</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/icono_washly</item>
        <item name="postSplashScreenTheme">@style/Theme.Wahsly</item>
    </style>

</resources>